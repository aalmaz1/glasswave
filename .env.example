# GlassWave Firebase configuration (copy to `.env`)
#
# These values are required for the web app to start. They are NOT committed —
# see `.gitignore`. Without them the app shows a configuration error instead of
# silently connecting to an unrelated Firebase project.

VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
